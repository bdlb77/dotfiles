# opencode/.config/opencode

Opencode global configuration.

- `opencode.jsonc` — main config (agents, permissions, MCP servers, etc.)
- `package.json` — opencode plugins (installed via `npm install` or `bun install`)

To install plugins after stowing:

```sh
cd ~/.config/opencode && bun install
```
